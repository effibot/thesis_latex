% Sezione 6: Conclusioni e Sviluppi Futuri
\section{Conclusioni e Sviluppi Futuri}


\begin{frame}
    \frametitle{Conclusioni}
    Rispetto ai requisiti iniziali, il progetto proposto ha raggiunto i seguenti obiettivi:
    \begin{itemize}
        \item Dimensionamento dei componenti meccanici ed elettrici rispetto alle specifiche di carico e prestazioni richieste.
        \item Selezione e acquisto dei materiali da diversi fornitori, acquisendo contatti per collaborazioni future.
        \item Progettazione e realizzazione di un dimostratore, con predisposizione per diversi layout di configurazione.
        \item Proposta di un'architettura di controllo per l'inseguimento di traiettorie.
        \item Sviluppo di un'architettura software per la comunicazione con gli inverter delle motoruote mediante adattatore USB, trasparente ad implementazioni CANOpen.
    \end{itemize}
\end{frame}
\begin{frame}
    \frametitle{Stato Attuale: Software}
    \begin{itemize}
        \item Libreria di comunicazione con adattatore USB-CAN-A ampiamente testata.
        \item Architettura software definita e parzialmente implementata.
              \begin{itemize}
                  \item Mancanti: Nodi ROS2 per generazione traiettorie e invio comandi.
              \end{itemize}
        \item Nodi ROS2 per l'avvio e la gestione dell'architettura di comunicazione, con pubblicazione dello stato dei motori e lettura dei sensori.
        \item Simulazione in Python dell'architettura di controllo per l'inseguimento di traiettorie.
    \end{itemize}
\end{frame}
\begin{frame}
    \frametitle{Stato Attuale: Hardware}
    \begin{columns}
        \begin{column}{0.5\textwidth}
            \begin{itemize}
                \item Acquisto di tutto l'hardware elencato completato.
                \item Proposto schema di cablaggio per l'integrazione dei sistemi su un banco di prova.
                \item Realizzazione del banco di prova per la validazione dei sistemi e dell'architettura software.
            \end{itemize}
        \end{column}
        \begin{column}{0.5\textwidth}
            \begin{figure}
                \centering
                \includegraphics[width=0.4\textwidth]{img/banco_prova.jpeg}
                \caption{Banco di test per una motoruota.}
            \end{figure}
            \vspace{-1cm}
            \begin{figure}
                \centering
                \includegraphics[width=0.55\textwidth]{img/carrello.jpeg}
                \caption{Carrello e lamiere}
            \end{figure}
        \end{column}
    \end{columns}
\end{frame}
\begin{frame}
    \frametitle{Sviluppi Futuri}
    \begin{enumerate}
        \item \textbf{Implementazione Hardare e Software}
              \begin{itemize}
                  \item Completare e integrare lo stack software completo su robot fisico,
                  \item Assemblaggio e cablaggio dei componenti hardware.
              \end{itemize}

        \item \textbf{Validazione Sperimentale}
              \begin{itemize}
                  \item Test su campo presso stabilimento Daikin Ariccia,
                  \item Misurazione prestazioni: precisione tracking, autonomia, affidabilità,
                  \item Ottimizzazione parametri controllo su dati reali,
              \end{itemize}

        \item \textbf{Estensioni}
              \begin{itemize}
                  \item Fleet management per coordinamento multi-robot,
                  \item Integrazione con MES aziendale per pianificazione trasporti,
                  \item Sensori addizionali: telecamere RGB-D, safety laser scanner.
                  \item Test di altri modelli cinematici e/o dinamici e architetture di controllo avanzate.
              \end{itemize}
    \end{enumerate}
\end{frame}
\begin{frame}
    \frametitle{Grazie per l'attenzione!}
    \begin{figure}
        \centering
        \includegraphics[width=0.4\textwidth]{img/3/mockup_agv.jpeg}
        \caption{Mockup del veicolo progettato; nome in codice: Cocco}
    \end{figure}
\end{frame}

\begin{frame}
    \frametitle{Appendice: IK Biciclo -- Centro Inst. di Rotazione (ICR)}
    \textbf{Concetto}: punto attorno al quale il veicolo sta ruotando in un dato istante

    \vspace{0.3cm}

    L'ICR si trova all'\textbf{intersezione degli assi di rotazione delle ruote} (perpendicolari alla direzione di avanzamento):

    \begin{equation}
        \begin{bmatrix}
            x_{ICR} \\ y_{ICR}
        \end{bmatrix}
        = \mathbf{p}_f^b + s_f \begin{bmatrix}
            -\sin(\delta_f) \\ \cos(\delta_f)
        \end{bmatrix}= \mathbf{p}_r^b + s_r \begin{bmatrix}
            -\sin(\delta_r) \\ \cos(\delta_r)
        \end{bmatrix}
    \end{equation}

    Risolvendo per $s_f$, $s_r$ si trovano i raggi di curvatura delle ruote:
    \begin{align}
        R_f = \sqrt{\left(x_{ICR} - \frac{L}{2}\right)^2 + \left(y_{ICR} \right)^2}, \qquad
        R_r = \sqrt{\left(x_{ICR} + \frac{L}{2}\right)^2 + \left(y_{ICR} \right)^2}
    \end{align}

    \textbf{Condizione di compatibilità} (moto senza slittamento):
    \begin{equation}
        \dot{\theta}= r\frac{\omega_f}{R_f}=r\frac{\omega_r}{R_r} \quad \Rightarrow \quad R_r \omega_f = R_f \omega_r
    \end{equation}

    \small{Caso $\delta_f= \delta_r$: ICR $\to \infty$, traslazione pura con $\omega_f = \omega_r$}
\end{frame}

\begin{frame}
    \frametitle{Appendice: IK Biciclo}
    \textbf{Problema}: dato $(\dot{x}_d, \dot{y}_d, \dot{\theta}_d)$, calcolare $(\delta_f, \delta_r, \omega_f, \omega_r)$

    \vspace{0.3cm}

    \textbf{Passo 1}: Calcolare velocità e side-slip desiderati
    \begin{align}
        v_d     & = \sqrt{\dot{x}_d^2 + \dot{y}_d^2}       \\
        \beta_d & = \atanyx{\dot{y}_d}{\dot{x}_d} - \theta
    \end{align}

    \textbf{Passo 2}: Definendo $S = \tan(\beta_d)$, $D = \frac{l\dot{\theta}_d}{v_d \cos(\beta_d)}$, da (\ref{eq:bicycle}) e (\ref{eq:beta}):
    \begin{equation}
        \begin{cases}
            lS = l_f \tan(\delta_r) + l_r \tan(\delta_f) \\
            D = \tan(\delta_f) - \tan(\delta_r)
        \end{cases}\Rightarrow
        \begin{aligned}
            \delta_f & = \arctan\left(S + \frac{l_f}{l} D\right) \\
            \delta_r & = \arctan\left(S - \frac{l_r}{l} D\right)
        \end{aligned}
    \end{equation}
    \textbf{Passo 3}: Calcolare $\omega_f$, $\omega_r$ da raggi ICR $R_f$, $R_r$ e condizione di compatibilità
\end{frame}
\begin{frame}
    \frametitle{Appendice: IK Biciclo -- Caso $\dot{\theta}_d = 0$ (Parte 1)}
    \textbf{Derivazione per traslazione pura}

    \vspace{0.3cm}

    Quando $\dot{\theta}_d = 0$, l'ICR $\to \infty$ e le direzioni di avanzamento delle ruote diventano parallele.

    \vspace{0.3cm}

    \textbf{Step 1}: Imporre $\delta_f = \delta_r = \delta$ (sterzi paralleli)

    \vspace{0.3cm}

    \textbf{Step 2}: Dalla relazione del side-slip angle:
    \begin{equation}
        \beta_d = \tan^{-1}\left(\frac{l_f + l_r}{l_f + l_r}\tan(\delta)\right) = \delta
    \end{equation}

    \vspace{0.3cm}

    Quindi il side-slip angle coincide con l'angolo di sterzo quando le ruote sono parallele.
\end{frame}

\begin{frame}
    \frametitle{Appendice: IK Biciclo -- Caso $\dot{\theta}_d = 0$ (Parte 2)}
    \textbf{Calcolo angoli di sterzo e velocità}

    \vspace{0.3cm}

    \textbf{Step 3}: Combinando $\beta_d = \delta$ con $\beta_d = \atanyx{\dot{y}_d}{\dot{x}_d} - \theta$:
    \begin{equation}
        \delta_f = \delta_r = \delta = \atanyx{\dot{y}_d}{\dot{x}_d} - \theta
    \end{equation}

    \vspace{0.3cm}

    \textbf{Step 4}: Le velocità angolari sono uguali (condizione di compatibilità per ICR $\to \infty$):
    \begin{equation}
        \omega_f = \omega_r = \frac{v_d}{r} = \frac{\sqrt{\dot{x}_d^2 + \dot{y}_d^2}}{r}
    \end{equation}
\end{frame}

\begin{frame}
    \frametitle{Appendice: IK Doppia Ruota Sterzante}
    \textbf{Problema}: dato $(\dot{x}_d, \dot{y}_d, \dot{\theta}_d)$, calcolare $(\delta_f, \delta_r, \omega_f, \omega_r)$

    $\Rightarrow$\textbf{Soluzione diretta dai vincoli di non-slittamento e puro rotolaento}:

    \textbf{Angoli di sterzo} (dalle eq. non-slip):
    \begin{align}
        \delta_f & = \atanyx{\dot{y}_d + \dot{\theta}_d \frac{L}{2}}{\dot{x}_d - \dot{\theta}_d \frac{W}{2}} \\
        \delta_r & = \atanyx{\dot{y}_d - \dot{\theta}_d \frac{L}{2}}{\dot{x}_d + \dot{\theta}_d \frac{W}{2}}
    \end{align}

    \textbf{Velocità angolari ruote} (dalle eq. rotolamento):
    \begin{align}
        \omega_f & = \frac{(\dot{y}_d + \dot{\theta}_d \frac{L}{2}) \sin(\delta_f) + (\dot{x}_d - \dot{\theta}_d \frac{W}{2}) \cos(\delta_f)}{r} \\
        \omega_r & = \frac{(\dot{y}_d - \dot{\theta}_d \frac{L}{2}) \sin(\delta_r) + (\dot{x}_d + \dot{\theta}_d \frac{W}{2}) \cos(\delta_r)}{r}
    \end{align}
    \textbf{Caso speciale $\dot{\theta}_d = 0$}: $\delta_f = \delta_r = \atanyx{\dot{y}_d}{\dot{x}_d}$, $\omega_f = \omega_r = \frac{v_d}{r}$
\end{frame}
